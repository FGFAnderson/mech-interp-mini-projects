# torch_nn

A small PyTorch mini-project: generate 2D Gaussian-blob data, train a basic MLP classifier on it, and plot the decision boundary.

See [NeuralNet.ipynb](NeuralNet.ipynb) for the full walkthrough.

## Setup

```bash
uv sync
```
